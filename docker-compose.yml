services:
  db:
    image: mysql:8.0
    container_name: timtro_mysql
    restart: always
    environment:
      MYSQL_DATABASE: 'timtro_db'
      MYSQL_ROOT_PASSWORD: 'admin_password_123'
    ports:
      - '3307:3306'
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: